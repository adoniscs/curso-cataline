export const sum = (a, b) => {
  return `O resultado da soma de ${a} + ${b} é igual a ${Number(a) + Number(b)}`
}

export const diference = (a, b) => {
  return `O resultado da diferença de ${a} - ${b} é igual a ${
    Number(a) - Number(b)
  }`
}
